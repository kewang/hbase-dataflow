table.table.table-bordered.table-condensed.table-hover
  tr
    th Row Key
    th(ng-repeat="c in table.getFullCQs()") {{ c }}
  tr(ng-repeat="k in table.getFullKeys()")
    th {{ k.key }}
    td(ng-repeat="v in k.cqs track by $index") {{ v }}
