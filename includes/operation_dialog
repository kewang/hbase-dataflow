
<div class="modal-header">
  <h4 class="modal-title">{{ operation.getTitle() }}</h4>
</div>
<div class="modal-body">
  <form role="form" class="form-horizontal">
    <div ng-show="operation.getSummary() != null" class="form-group">
      <label class="col-md-2 control-label">Summary</label>
      <div class="col-md-10">
        <p class="form-control-static">{{ operation.getSummary() }}</p>
      </div>
    </div>
    <hr ng-show="operation.getSummary() != null"/>
    <div class="form-group">
      <label class="col-md-2 control-label">Table</label>
      <div class="col-md-10">
        <p class="form-control-static">{{ operation.getTable() }}</p>
      </div>
    </div>
    <div class="form-group">
      <label class="col-md-2 control-label">Key</label>
      <div class="col-md-10">
        <p class="form-control-static">{{ operation.getKey() }}</p>
      </div>
    </div>
    <h5>Create CQ</h5>
    <table class="table">
      <tr>
        <th ng-repeat="cq in operation.getCreateCQs()">{{ cq.name }}</th>
      </tr>
      <tr>
        <td ng-repeat="cq in operation.getCreateCQs()">{{ cq.value }}</td>
      </tr>
    </table>
    <h5>Update CQ</h5>
    <table class="table">
      <tr>
        <th ng-repeat="cq in operation.getUpdateCQs()">{{ cq.name }}</th>
      </tr>
      <tr>
        <td ng-repeat="cq in operation.getUpdateCQs()"><span>{{ cq.oldvalue }}</span><span>{{ cq.newvalue }}</span></td>
      </tr>
    </table>
  </form>
</div>
<div class="modal-footer">
  <button type="button" data-dismiss="modal" class="btn btn-default">Close</button>
</div>